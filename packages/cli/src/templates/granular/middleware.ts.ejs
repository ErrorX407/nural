import { defineMiddleware } from "nural";

export const <%= camelName %>Middleware = defineMiddleware(async (req, res) => {
  // 💡 Your Logic Here
  const header = req.headers["x-custom-header"];

  // Return data to extend the request context
  return {
    <%= camelName %>Data: header || "default"
  };
});