import { createModule } from "@nuraljs/core";
import { loginRoute, registerRoute } from "./auth.controller";

export const authModule = createModule({
  prefix: "/auth",
  tags: ["Auth"],
  routes: [loginRoute, registerRoute],
});