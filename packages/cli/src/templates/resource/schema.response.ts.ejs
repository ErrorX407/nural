import { Schema } from "@nuraljs/core";

export const <%= className %>Response = Schema.object({
  id: Schema.string(),
  name: Schema.string(),
  createdAt: Schema.string().datetime(),
  updatedAt: Schema.string().datetime(),
  });

  export type <%= className %>Response = Schema.infer<typeof <%=className %>Response>;