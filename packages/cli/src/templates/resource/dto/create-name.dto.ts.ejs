import { Schema } from "@nuraljs/core";

export const Create<%= className %>DTO = Schema.object({
    name: Schema.string().min(2).openapi({ example: "New <%= className %>" }),
        });

        export type Create<%= className %>DTO = Schema.infer<typeof Create<%=className %>DTO>;